-- DBSafe seed data.
-- The first administrator account is created by the application (or by
-- scripts/initialize_db.py) because its password must be hashed in Python.
-- Nothing else needs to be seeded.
SELECT 1;
